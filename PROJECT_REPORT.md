# گزارش جامع اقدامات، عیب‌یابی‌ها و بهبودهای فنی سیستم مشاوره هوشمند بیمه

این سند شامل گزارش کاملی از عیب‌یابی‌های انجام‌شده، ریشه‌یابی مشکلات اولیه و راهکارهای فنی اعمال‌شده در سیستم مشاوره و خرید آنلاین بیمه (مبتنی بر Graphiti، دیتابیس گرافی Neo4j و Google Gemini) است.

---

## ۱. جدول مشکلات شناسایی‌شده و علل ریشه‌ای (Root Causes)

| ردیف | عنوان مشکل اولیه | علت فنی ریشه‌ای در کد |
| :--- | :--- | :--- |
| **۱** | **نشت حافظه و داده‌های اشتباه بین نشست‌ها** (نسبت دادن اطلاعات چت‌های قبلی مثل شهر تهران یا سال ۱۳۹۴ به کاربر جدید) | در متد get_user_longterm_memory_context واقع در GraphitiMemoryService، برای کاربران مهمان یا چت‌های بدون کاربر مشخص به اشتباه از ChatSession.objects.all()[:3] استفاده می‌شد که داده‌های چت‌های سایر کاربران را می‌خواند و به پرامپت تزریق می‌کرد. |
| **۲** | **نمایش کد خام JSON درون چت و عدم رندر کارت خرید** | سیستم از یک Regex محدود استفاده می‌کرد که فقط بلوک‌های با علامت `json را می‌شناخت؛ اگر هوش مصنوعی JSON را بدون بک‌تیک تولید می‌کرد، کد خام در صفحه چت باقی می‌ماند و کارت استعلام ساخته نمی‌شد. |
| **۳** | **کندی و تاخیر در پاسخ‌دهی هوش مصنوعی** | کتابخانه گوگل با خطاهای ۵۰۳ (ترافیک سرور) و ۴۲۹ مواجه شده و وارد چرخه‌های تکرار طولانی (Retry با تاخیر چند ثانیه‌ای) می‌شد؛ همچنین اتصال به گراف و بازرتبه‌بندی مجدد چند ثانیه زمان می‌برد. |
| **۴** | **تعداد اپیزودهای خرد و پراکنده در گراف (۶ اپیزود)** | اطلاعات فایل‌های سه‌گانه JSON به ۶ اپیزود خرد شکسته شده بود که پیوستگی روابط شرکت‌ها و پوشش‌های هر محصول را پراکنده می‌کرد. |
| **۵** | **پاسخ‌های پیش‌فرض (Fallback) نامربوط در زمان قطعی یا سوالات کاربر** | متد Fallback به جای پاسخ به سوال کاربر (مثلاً سوال درباره بیمه‌نامه خریداری‌شده)، به صورت پیش‌فرض پارامترهای ناقص خودرو مانند «چند سال سابقه تخفیف عدم خسارت دارید؟» را تکرار می‌کرد. |
| **۶** | **عدم تشخیص شهرهای جدید و تداخل نوع بیمه** | لیست شهرها به ۲۵ مرکز استان محدود بود (شهرهایی مثل بانه، سقز، کیش و... تشخیص داده نمی‌شدند) و با ارسال مدل خودرو، نوع بیمه از بدنه به شخص ثالث تغییر می‌یافت. |
| **۷** | **استفاده از عناوین هشتگ‌دار (##)، ایموجی و عبارات کلیشه‌ای** | خروجی مدل شامل علامت‌های مارک‌داون هشتگ، ایموجی و جملات کلیشه‌ای مثل «بر اساس اطلاعات موجود در پایگاه دانش...» و اصرار ناخواسته به خرید در انتهای هر پیام بود. |

---

## ۲. اصلاحات و راهکارهای فنی پیاده‌سازی‌شده

### الف) ساختاردهی به دقیقاً ۳ اپیزود یکپارچه در پایگاه دانش گراف (Neo4j)
منطق دستور ingest_knowledge.py بازنویسی شد تا تمام داده‌های ۳ فایل مرجع در **دقیقاً ۳ اپیزود جامع** به دیتابیس گرافی Neo4j تزریق شوند:
1. **اپیزود ۱ (insurance_knowledge.json):** پایگاه دانش انواع بیمه، پوشش‌های پایه و تکمیلی، عوامل قیمت‌گذاری و سوالات استعلام.
2. **اپیزود ۲ (insurance_companies.json):** پایگاه دانش شرکت‌های بیمه شامل محصولات، کلیه پوشش‌های اختصاصی هر شرکت (بیمه رازی و بیمه ایران) و شرایط اقساط و چک.
3. **اپیزود ۳ (consultation_knowledge.json):** راهنمای جامع مشاوره، سناریوهای مقایسه‌ای و پرسش‌های متداول مشتریان.

### ب) بهینه‌سازی سرعت واکشی گراف (Sub-10ms) و مدل Gemini
- در کلاس GraphitiService درایور ارتباطی پایگاه داده Neo4j به صورت یک درایور پایدار (Singleton Driver) پیاده‌سازی شد.
- واکشی روابط و فکت‌های گراف با کوئری‌های بهینه Cypher مستقیماً در **زیر ۱۰ میلی‌ثانیه** انجام می‌شود.
- سقف زمانی ارتباط (Timeout) برای مدل هوش مصنوعی (gemini-3.5-flash-lite) تنظیم گردید تا هرگز معطلی در شبکه ایجاد نشود.

### ج) تصحیح رندر کارت پیشنهاد خرید با تحلیل‌گر براکت‌های متوازن (Balanced-Brace JSON Parser)
- در backend/chat/views.py الگوریتم استخراج با یک تحلیل‌گر براکت متوازن پیاده‌سازی شد.
- هرگونه بلوک JSON مربوط به پیشنهاد خرید (چه با مارک‌داون و چه به صورت متن خام) به صورت ۱۰۰٪ از متن پیام چت پاک‌سازی شده و به عنوان آبجکت گرافیکی کارت استعلام و خرید به فرانت‌اند ارسال می‌شود.

### د) اصلاح استخراج پارامترها و ایزوله‌سازی حافظه نشست‌ها
- لیست تمامی شهرستان‌ها و شهرهای کشور به همراه الگوی Regex تشخیص هوشمند شهر، سال ساخت و سابقه تخفیف اضافه گردید.
- نوع بیمه انتخاب‌شده کاربر در طول گفتگو تثبیت شد و با ورود مدل خودرو بازنویسی نمی‌شود.
- حافظه هر گفتگوی جدید برای کاربران مهمان کاملاً مستقل شد و از سوابق سایر نشست‌ها جدا گردید.

### ه) بهینه‌سازی لحن و رفتار هوشمند مشاور بیمه
- **پاسخ‌های کوتاه و خلاصه:** پاسخ‌های مشاوره‌ای در ۲ الی ۳ خط مفید و بدون زیاده‌گویی ارائه می‌شوند.
- **حذف درخواست‌های ناخواسته اطلاعات خودرو:** در حین مشاوره، هوش مصنوعی بدون تقاضای کاربر درخواست ۴ پارامتر نمی‌کند؛ فقط در صورتی اطلاعات خودرو را می‌پرسد که کاربر صراحتاً قصد استعلام نرخ یا خرید داشته باشد.
- **حذف کامل ایموجی‌ها و هشتگ‌ها (##):** خروجی به صورت متن ساده و لیست‌های خط‌تیره‌ای تمیز رندر می‌شود.
- **حذف عبارات کلیشه‌ای:** هرگونه عبارت نظیر «بر اساس اطلاعات موجود در پایگاه دانش» با فیلتر خودکار و دستورالعمل پرامپت حذف گردید.
- **پاسخگویی به وضعیت بیمه‌نامه خریداری‌شده:** در صورتی که کاربر درباره بیمه‌نامه خریداری‌شده‌اش سوال بپرسد، اطلاعات دقیق شماره سفارش و طرح مستقیماً از دیتابیس گزارش می‌شود.

---

## ۳. وضعیت اجرای سرویس‌ها

- **فرانت‌اند (React + Vite):** http://localhost:5173
- **بک‌اند (Django REST Framework):** http://127.0.0.1:8000
- **دیتابیس گرافی (Neo4j):** bolt://localhost:7687 (Graph Database با ۳ اپیزود ساختاریافته)